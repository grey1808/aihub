<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Настройки базы знаний (RAG)
    |--------------------------------------------------------------------------
    */

    // Размер куска документа в символах и нахлёст между кусками.
    // Нахлёст нужен, чтобы мысль, разорванная на границе, не потерялась.
    'chunk_size'    => (int) env('RAG_CHUNK_SIZE', 1200),
    'chunk_overlap' => (int) env('RAG_CHUNK_OVERLAP', 200),

    // Сколько кусков подкладываем модели в контекст при ответе.
    'top_k' => (int) env('RAG_TOP_K', 6),

    // Минимальная похожесть (0..1). Всё, что ниже — считаем мусором
    // и не показываем модели, чтобы она не выдумывала на пустом месте.
    'min_score' => (float) env('RAG_MIN_SCORE', 0.35),

    // Максимальный размер файла, который берём в индекс.
    'max_file_size' => (int) env('RAG_MAX_FILE_SIZE', 52428800), // 50 МБ

    // Какие расширения умеем читать.
    'extensions' => ['txt', 'md', 'pdf', 'doc', 'docx', 'rtf', 'odt', 'csv', 'html', 'htm', 'xml', 'json'],

    // Искать по базе знаний автоматически перед каждым вопросом.
    // Выключать стоит только если модель уверенно сама вызывает инструменты
    // и хочется экономить время на лишнем поиске.
    'auto_context' => (bool) env('RAG_AUTO_CONTEXT', true),

    // Сколько документов за один проход синхронизации класть в очередь.
    'sync_batch' => (int) env('RAG_SYNC_BATCH', 50),
];
